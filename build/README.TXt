This directory contains project templates to compile the BCILAB toolbox into a standalone binary that only requires the MATLAB Runtime Engine to be installed. 
Pre-compiled binaries for various platforms are in the distrib/ folder, but are updated only for major releases.
